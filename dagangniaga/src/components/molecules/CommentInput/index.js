import React from "react";
import {View, Text} from "react-native";

const CommentInput = ({postId}) => {
    return (
        <View>
            <Text>CommentInput {postId}</Text>
        </View>
    )
}

export default CommentInput;